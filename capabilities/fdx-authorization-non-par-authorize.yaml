naftiko: 1.0.0-alpha2
info:
  label: Mastercard Developer Hub for FDX APIs FDX Authorization Server — Non PAR Authorize
  description: 'Mastercard Developer Hub for FDX APIs FDX Authorization Server — Non PAR Authorize. 1 operations. Lead operation:
    Mastercard Developer Hub for FDX APIs User Authorization Endpoint. Self-contained Naftiko capability covering one Mastercard
    Developer Hub For Fdx Apis business surface.'
  tags:
  - Mastercard Developer Hub For Fdx Apis
  - Non PAR Authorize
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_DEVELOPER_HUB_FOR_FDX_APIS_API_KEY: MASTERCARD_DEVELOPER_HUB_FOR_FDX_APIS_API_KEY
capability:
  consumes:
  - type: http
    namespace: fdx-authorization-non-par-authorize
    baseUri: http://localhost:8080
    description: Mastercard Developer Hub for FDX APIs FDX Authorization Server — Non PAR Authorize business capability. Self-contained,
      no shared references.
    resources:
    - name: oauth2-authorize
      path: /oauth2/authorize
      operations:
      - name: parauthorizedonthitcopytheurlfromcurl
        method: GET
        description: Mastercard Developer Hub for FDX APIs User Authorization Endpoint
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MASTERCARD_DEVELOPER_HUB_FOR_FDX_APIS_API_KEY}}'
  exposes:
  - type: rest
    namespace: fdx-authorization-non-par-authorize-rest
    port: 8080
    description: REST adapter for Mastercard Developer Hub for FDX APIs FDX Authorization Server — Non PAR Authorize. One
      Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/oauth2/authorize
      name: oauth2-authorize
      description: REST surface for oauth2-authorize.
      operations:
      - method: GET
        name: parauthorizedonthitcopytheurlfromcurl
        description: Mastercard Developer Hub for FDX APIs User Authorization Endpoint
        call: fdx-authorization-non-par-authorize.parauthorizedonthitcopytheurlfromcurl
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fdx-authorization-non-par-authorize-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Developer Hub for FDX APIs FDX Authorization Server — Non PAR Authorize. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: mastercard-developer-hub-fdx-apis
      description: Mastercard Developer Hub for FDX APIs User Authorization Endpoint
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fdx-authorization-non-par-authorize.parauthorizedonthitcopytheurlfromcurl
      outputParameters:
      - type: object
        mapping: $.
