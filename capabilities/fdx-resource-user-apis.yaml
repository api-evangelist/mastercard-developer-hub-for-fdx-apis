naftiko: 1.0.0-alpha2
info:
  label: Mastercard Developer Hub for FDX APIs Fdx Resource API — User APIs
  description: 'Mastercard Developer Hub for FDX APIs Fdx Resource API — User APIs. 2 operations. Lead operation: Mastercard
    Developer Hub for FDX APIs Save user. Self-contained Naftiko capability covering one Mastercard Developer Hub For Fdx
    Apis business surface.'
  tags:
  - Mastercard Developer Hub For Fdx Apis
  - User APIs
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MASTERCARD_DEVELOPER_HUB_FOR_FDX_APIS_API_KEY: MASTERCARD_DEVELOPER_HUB_FOR_FDX_APIS_API_KEY
capability:
  consumes:
  - type: http
    namespace: fdx-resource-user-apis
    baseUri: http://localhost:9090
    description: Mastercard Developer Hub for FDX APIs Fdx Resource API — User APIs business capability. Self-contained, no
      shared references.
    resources:
    - name: user
      path: /user
      operations:
      - name: saveuser
        method: POST
        description: Mastercard Developer Hub for FDX APIs Save user
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: This field indicate the Access Token id
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: user-userId
      path: /user/{userId}
      operations:
      - name: getuser
        method: GET
        description: Mastercard Developer Hub for FDX APIs Get user by user Id
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
          description: This field indicate the Access Token id
          required: true
        - name: userId
          in: path
          type: string
          description: This field indicate the user ID
          required: true
    authentication:
      type: apikey
      key: Authorization
      value: '{{env.MASTERCARD_DEVELOPER_HUB_FOR_FDX_APIS_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: fdx-resource-user-apis-rest
    port: 8080
    description: REST adapter for Mastercard Developer Hub for FDX APIs Fdx Resource API — User APIs. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/user
      name: user
      description: REST surface for user.
      operations:
      - method: POST
        name: saveuser
        description: Mastercard Developer Hub for FDX APIs Save user
        call: fdx-resource-user-apis.saveuser
        with:
          Authorization: rest.Authorization
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/user/{userid}
      name: user-userid
      description: REST surface for user-userId.
      operations:
      - method: GET
        name: getuser
        description: Mastercard Developer Hub for FDX APIs Get user by user Id
        call: fdx-resource-user-apis.getuser
        with:
          Authorization: rest.Authorization
          userId: rest.userId
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: fdx-resource-user-apis-mcp
    port: 9090
    transport: http
    description: MCP adapter for Mastercard Developer Hub for FDX APIs Fdx Resource API — User APIs. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: mastercard-developer-hub-fdx-apis
      description: Mastercard Developer Hub for FDX APIs Save user
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: fdx-resource-user-apis.saveuser
      with:
        Authorization: tools.Authorization
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: mastercard-developer-hub-fdx-apis-2
      description: Mastercard Developer Hub for FDX APIs Get user by user Id
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: fdx-resource-user-apis.getuser
      with:
        Authorization: tools.Authorization
        userId: tools.userId
      outputParameters:
      - type: object
        mapping: $.
